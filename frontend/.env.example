VITE_USE_MOCK_API=false
# Optional API origin; empty uses same-origin /api and the Vite development proxy.
VITE_API_BASE_URL=
# Implemented endpoints; set false only when using a backend without them.
VITE_ENABLE_REVIEWS=true
VITE_ENABLE_ZIP_EXPORT=true
