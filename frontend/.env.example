VITE_USE_MOCK_API=false
# Optional API origin; empty uses same-origin /api and the Vite development proxy.
VITE_API_BASE_URL=
# Planned optional endpoints; enable once the backend implements them.
VITE_ENABLE_REVIEWS=false
VITE_ENABLE_ZIP_EXPORT=false
